import { createSlug } from './slug.js';
import { validateField, checkConfirmationPwd } from './validateField.js';
import { formatPostDate, formatCommentDate } from './dateFormat.js';

export default {
  createSlug,
  validateField,
  checkConfirmationPwd,
  formatPostDate,
  formatCommentDate,
};
